import { eventTabBadgeType } from '@/types/InitialDataTypes'
import React from 'react'
import { Button } from '../ui/button'

export default function EventTabItem({
  eventTabBageData,
}: {
  eventTabBageData: eventTabBadgeType
}) {
  return <Button>test</Button>
}
