interface QuickMenuBadgeType {
  id: number
  targetUrl: string
  categoryName: string
  categoryBadgeImageUrl: string
}

interface CategoryType {
  categoryId: number
  categoryName: string
  depth: number
}
